# Huxy Node Server - 环境变量配置示例
# 
# 复制此文件为 .env 并根据需要修改配置
# 所有配置也可以通过命令行参数传递

# ============================================
# 基本配置
# ============================================

# 环境: development, production, test
NODE_ENV=development

# 服务器端口
PORT=3000

# 服务器主机地址
HOST=0.0.0.0

# 基础路径（例如 /api）
BASEPATH=/

# ============================================
# 安全配置
# ============================================

# CORS 允许的来源，多个用逗号分隔，使用 * 允许所有
CORS_ORIGIN=http://localhost:3000,http://localhost:8080

# 速率限制 - 时间窗口（毫秒）
RATE_LIMIT_WINDOW_MS=900000

# 速率限制 - 最大请求数
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# 日志配置
# ============================================

# 日志级别: 10 (trace), 20 (debug), 30 (info), 40 (warn), 50 (error), 60 (fatal)
LOG_LEVEL=30

# ============================================
# 高级配置
# ============================================

# API 前缀（可选）
API_PREFIX=/api

# JWT 秘钥（用于未来身份验证）
JWT_SECRET=your-secret-key-here
# JWT 秘钥过期时间
JWT_EXPIRES_IN=1d

# 身份验证令牌
AUTH_TOKEN=1234

# ============================================
# 数据库配置（示例）
# ============================================

# MongoDB 连接字符串
# MONGODB_URI=mongodb://localhost:27017/huxy-server

# Redis 连接字符串
# REDIS_URL=redis://localhost:6379

# ============================================
# 静态文件服务配置
# ============================================

# 静态文件服务端口（如果使用 startStatic）
# STATIC_PORT=9000

# 静态文件构建路径
# BUILD_PATH=./build

# 静态文件基础路径
# BASEPATH=/